package com.bconlon.tombsandtreasures.common.items.ranged.bow;

import com.bconlon.tombsandtreasures.common.items.ranged.AbstractBowItem;

public class BowLootItem extends AbstractBowItem
{
    public BowLootItem(float rangedDamageBonus, int useDuration, float pullbackTime, float velocityStrength, float inaccuracy, Properties properties) {
        super(rangedDamageBonus, useDuration, pullbackTime, velocityStrength, inaccuracy, properties);
    }
}
